# Global Instructions

## Applying config changes

When the task is to disable or configure something by name (e.g. an MCP server, a plugin), apply the setting directly — don't spend time tracing where it's configured. The setting works by name. Only investigate origin if the task explicitly requires it.
